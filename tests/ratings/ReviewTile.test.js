import React from 'react';
import { render, fireEvent } from '@testing-library/react';
import ReviewTile from '../../client/src/Ratings/ReviewTile';

test('ReviewTile renders', () => {
  // var { getByTestId } = render(<ReviewTile/>);
  // var divEl = getByTestId('practice-div');
  // expect(divEl.textContent).toBe('This is a Practice component');
})